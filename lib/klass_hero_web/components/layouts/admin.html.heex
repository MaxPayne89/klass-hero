<Backpex.HTML.Layout.app_shell
  socket={@socket}
  fluid={@fluid?}
  live_resource={@live_resource}
  sidebar_open={@sidebar_open}
  preferences_manifest={@preferences_manifest}
>
  <%!-- sidebar_branding is a fixed h-14 row in a 256px sidebar, not the full-width topbar
        this used to live in. The logo is already a "Klass Hero" wordmark, so the old
        "Klass Hero Admin" title both repeated the brand and wrapped to three lines,
        overflowing the row. Shorter title + smaller wordmark keeps it on one line. --%>
  <:sidebar_branding>
    <Backpex.HTML.Layout.sidebar_branding title={gettext("Admin")}>
      <:logo>
        <img src={~p"/images/logo.png"} alt={gettext("Klass Hero")} class="h-6" />
      </:logo>
    </Backpex.HTML.Layout.sidebar_branding>
  </:sidebar_branding>

  <:topbar>
    <div class="flex-1"></div>

    <Backpex.HTML.Layout.topbar_dropdown>
      <:label>
        <div class="btn btn-square btn-ghost">
          <Backpex.HTML.CoreComponents.icon name="hero-user" class="size-6" />
        </div>
      </:label>
      <li>
        <.link navigate={~p"/"}>{gettext("Back to App")}</.link>
      </li>
      <li>
        <.link href={~p"/users/log-out"} method="delete" class="text-error">
          {gettext("Sign Out")}
        </.link>
      </li>
    </Backpex.HTML.Layout.topbar_dropdown>
  </:topbar>

  <:sidebar>
    <Backpex.HTML.Layout.sidebar_item current_url={@current_url} navigate={~p"/admin/accounts"}>
      <Backpex.HTML.CoreComponents.icon name="hero-users" class="h-5 w-5" /> {gettext("Accounts")}
    </Backpex.HTML.Layout.sidebar_item>
    <Backpex.HTML.Layout.sidebar_item current_url={@current_url} navigate={~p"/admin/providers"}>
      <Backpex.HTML.CoreComponents.icon name="hero-building-storefront" class="h-5 w-5" /> {gettext(
        "Providers"
      )}
    </Backpex.HTML.Layout.sidebar_item>
    <Backpex.HTML.Layout.sidebar_item current_url={@current_url} navigate={~p"/admin/staff"}>
      <Backpex.HTML.CoreComponents.icon name="hero-user-group" class="h-5 w-5" /> {gettext(
        "Staff Members"
      )}
    </Backpex.HTML.Layout.sidebar_item>
    <Backpex.HTML.Layout.sidebar_item current_url={@current_url} navigate={~p"/admin/bookings"}>
      <Backpex.HTML.CoreComponents.icon name="hero-ticket" class="h-5 w-5" /> {gettext("Bookings")}
    </Backpex.HTML.Layout.sidebar_item>
    <Backpex.HTML.Layout.sidebar_item current_url={@current_url} navigate={~p"/admin/consents"}>
      <Backpex.HTML.CoreComponents.icon name="hero-shield-check" class="h-5 w-5" /> {gettext(
        "Consents"
      )}
    </Backpex.HTML.Layout.sidebar_item>
    <Backpex.HTML.Layout.sidebar_item current_url={@current_url} navigate={~p"/admin/incidents"}>
      <Backpex.HTML.CoreComponents.icon name="hero-exclamation-triangle" class="h-5 w-5" /> {gettext(
        "Incidents"
      )}
    </Backpex.HTML.Layout.sidebar_item>
    <Backpex.HTML.Layout.sidebar_item
      current_url={@current_url}
      navigate={~p"/admin/undelivered-events"}
    >
      <Backpex.HTML.CoreComponents.icon name="hero-inbox-arrow-down" class="h-5 w-5" /> {gettext(
        "Undelivered Events"
      )}
    </Backpex.HTML.Layout.sidebar_item>
    <Backpex.HTML.Layout.sidebar_item current_url={@current_url} navigate={~p"/admin/emails"}>
      <Backpex.HTML.CoreComponents.icon name="hero-envelope" class="h-5 w-5" /> {gettext("Emails")}
      <span
        :if={assigns[:unread_email_count] && @unread_email_count > 0}
        class="ml-auto inline-flex items-center justify-center rounded-full bg-red-500 px-2 py-0.5 text-xs font-bold text-white"
      >
        {@unread_email_count}
      </span>
    </Backpex.HTML.Layout.sidebar_item>
    <Backpex.HTML.Layout.sidebar_item current_url={@current_url} navigate={~p"/admin/sessions"}>
      <Backpex.HTML.CoreComponents.icon name="hero-calendar-days" class="h-5 w-5" /> {gettext(
        "Sessions"
      )}
    </Backpex.HTML.Layout.sidebar_item>
    <Backpex.HTML.Layout.sidebar_item current_url={@current_url} navigate={~p"/admin/messages"}>
      <Backpex.HTML.CoreComponents.icon name="hero-chat-bubble-left-right" class="h-5 w-5" /> {gettext(
        "Messages"
      )}
    </Backpex.HTML.Layout.sidebar_item>
  </:sidebar>

  <Backpex.HTML.Layout.flash_messages flash={@flash} />
  <%!-- Trigger: Backpex passes empty named slot [] for inner_content; Phoenix layout passes rendered content --%>
  <%!-- Why: [] is truthy in Elixir, so plain `if assigns[:inner_content]` would render nothing for Backpex --%>
  <%!-- Outcome: Backpex resources use @inner_block slot; SessionsLive uses Phoenix layout @inner_content --%>
  <%= if assigns[:inner_content] not in [nil, []] do %>
    {@inner_content}
  <% else %>
    {render_slot(@inner_block)}
  <% end %>
</Backpex.HTML.Layout.app_shell>
